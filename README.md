<h1 align="center">🤖 Weibo Autopilot Generator</h1>

<p align="center">
  <strong>为 Claude Code 打造的微博自动化 Skill 生成器</strong>
</p>

<p align="center">
  <a href="#功能特性">功能特性</a> •
  <a href="#在线使用">在线使用</a> •
  <a href="#快速开始">快速开始</a> •
  <a href="#配置说明">配置说明</a> •
  <a href="#常见问题">常见问题</a>
</p>

---

## 功能特性

- 🎯 **个性化定制** - 根据你的微博风格生成专属 Skill
- 🤖 **智能学习** - 自动学习你的发帖风格和兴趣偏好
- 🔄 **自动转发** - 根据话题匹配自动转发感兴趣的内容
- 💬 **智能评论** - 基于你的评论风格自动生成评论
- 📊 **分组支持** - 可指定特定分组作为内容来源
- 🛡️ **安全标识** - 所有 AI 生成的内容带有明确标识

## 在线使用

访问 [Weibo Autopilot Generator](https://liubin1.github.io/weibo-autopilot-generator/) 在线生成你的个性化 Skill。

## 快速开始

### 方式一：使用在线生成器（推荐）

1. 访问 [在线生成器](https://liubin1.github.io/weibo-autopilot-generator/)
2. 填写配置信息
3. 下载生成的 Skill
4. 解压并移动到 `~/.agents/skills/` 目录
5. 按照下方说明运行

### 方式二：使用通用模板

```bash
# 下载通用模板
git clone https://github.com/liubin1/weibo-autopilot-generator.git
cd weibo-autopilot-generator

# 复制模板到 skills 目录
cp -r templates/weibo-autopilot ~/.agents/skills/my-weibo-autopilot

# 进入 skill 目录
cd ~/.agents/skills/my-weibo-autopilot/scripts

# 安装依赖（如果需要）
# bun 会自动处理 TypeScript

# 首次运行：学习用户偏好
bun learn-preferences.ts

# 启动自动化
bun autopilot.ts
```

## 配置说明

### 用户配置 (data/user-config.json)

```json
{
  "skillName": "my-weibo-autopilot",
  "weiboUser": "你的微博昵称或UID",
  "topics": ["科技", "足球", "国际新闻"],
  "learningSources": {
    "posts": true,
    "comments": true,
    "reposts": false,
    "likes": false
  },
  "actions": {
    "repost": true,
    "comment": false,
    "like": false
  },
  "contentSource": {
    "type": "home",
    "groups": []
  },
  "settings": {
    "intervalMinutes": 10,
    "addSignature": true
  }
}
```

### 配置项说明

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| `topics` | 感兴趣的话题，用于筛选转发内容 | `[]` |
| `learningSources.posts` | 是否学习主页发帖风格 | `true` |
| `learningSources.comments` | 是否学习评论风格 | `true` |
| `actions.repost` | 是否启用自动转发 | `true` |
| `contentSource.type` | 内容来源类型 (`home`/`group`) | `home` |
| `contentSource.groups` | 分组名称列表 | `[]` |
| `settings.intervalMinutes` | 执行间隔（分钟） | `10` |
| `settings.addSignature` | 是否添加 AI 标识 | `true` |

## 命令参考

```bash
# 学习用户偏好（首次必须运行）
bun learn-preferences.ts

# 启动自动化
bun autopilot.ts

# 指定分组
bun autopilot.ts --group "科技博主"

# 设置执行间隔
bun autopilot.ts --interval 15

# 试运行模式（不实际转发）
bun autopilot.ts --dry-run

# 手动转发指定帖子
bun repost.ts https://weibo.com/xxx/xxx --comment "好内容"

# 浏览并收集 feed
bun browse-feed.ts --group "足球"
```

## 工作原理

```
┌─────────────────────────────────────────────────────────────┐
│                    Weibo Autopilot                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. 学习阶段 (learn-preferences.ts)                         │
│     └── 分析用户主页、评论，生成用户画像                     │
│                                                             │
│  2. 浏览阶段 (browse-feed.ts)                               │
│     └── 浏览首页/分组，收集帖子                              │
│                                                             │
│  3. 筛选阶段 (autopilot.ts)                                 │
│     └── 根据话题和偏好打分，选择最佳内容                     │
│                                                             │
│  4. 执行阶段 (repost.ts)                                    │
│     └── 生成评论，执行转发                                   │
│                                                             │
│  5. 循环 (每 N 分钟重复 2-4)                                 │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 文件结构

```
weibo-autopilot/
├── scripts/
│   ├── weibo-cdp.ts          # Chrome DevTools Protocol 封装
│   ├── browse-feed.ts        # 浏览 Feed 并收集帖子
│   ├── repost.ts             # 转发功能
│   ├── learn-preferences.ts  # 学习用户偏好
│   └── autopilot.ts          # 主自动化脚本
├── data/
│   ├── user-config.json      # 用户配置
│   ├── user-profile.json     # 学习到的用户画像
│   └── repost-history.json   # 转发历史记录
├── skill.md                  # Skill 元数据
└── README.md
```

## 常见问题

### Q: 为什么需要 Chrome？
A: 本工具使用 Chrome DevTools Protocol 进行浏览器自动化，需要本地安装 Chrome 或 Chromium。

### Q: 首次运行需要做什么？
A: 首次运行时会打开浏览器，需要手动登录微博账号。登录后浏览器会保持登录状态。

### Q: 如何避免被识别为机器人？
A: 工具已内置随机化间隔时间、真实鼠标点击等机制。建议设置合理的间隔时间（10-30分钟）。

### Q: 转发的内容会有标识吗？
A: 是的，默认会在转发内容末尾添加 AI 标识。可以通过配置关闭。

### Q: 如何只浏览特定分组？
A: 在 `user-config.json` 中设置 `contentSource.type` 为 `"group"`，并在 `groups` 中填写分组名称。

### Q: 如何停止运行？
A: 在终端按 `Ctrl+C` 即可停止。

## 注意事项

1. **合理使用** - 请遵守微博平台规则，避免过于频繁的操作
2. **内容审核** - 建议先使用 `--dry-run` 模式测试
3. **账号安全** - 登录信息存储在本地浏览器配置文件中
4. **AI 标识** - 建议保持 AI 标识以透明化使用

## 贡献

欢迎提交 Issue 和 Pull Request！

## 许可证

MIT License

---

<p align="center">Made with ❤️ by Claude</p>
